# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Document loading
pypdf>=3.17.0
beautifulsoup4>=4.12.0

# Embeddings
sentence-transformers>=2.3.0
torch>=2.1.0

# Vector store
qdrant-client>=1.7.0

# Sparse retrieval
rank-bm25>=0.2.2

# Graph-based retrieval (Sprint 15 — GraphRAG community detection)
networkx>=3.2

# Generation backends
openai>=1.6.0
anthropic>=0.20.0
httpx>=0.25.0

# CLI
click>=8.1.0

# Evaluation (optional — heavy deps; install with: pip install ragas datasets)
# ragas>=0.1.0
# datasets>=2.15.0

# Observability (optional — Sprint 16; install with: pip install prometheus-client opentelemetry-sdk)
# prometheus-client>=0.19
# opentelemetry-sdk>=1.20
# opentelemetry-exporter-otlp-proto-grpc>=1.20

# Multi-tenancy JWT auth (optional — Sprint 17; install with: pip install PyJWT)
# PyJWT>=2.8

# MCP server transport (optional — Sprint 19; install with: pip install mcp)
# mcp>=1.0
